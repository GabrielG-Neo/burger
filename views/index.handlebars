<h1>Cheat Burgers</h1>

<h2>Cheaters Choice</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{burger_name}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
     {{burger_name}}
    {{/if}}
  {{/each}}
</ul>

<h2>Craving a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured<br>
  </div>

  <button type="submit" value="name"></button>Add Burger</button>
</form>

